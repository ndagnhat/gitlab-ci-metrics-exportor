{{ include "gitlab-ci-metrics-exporter.fullname" . }} has been deployed.

Service: {{ include "gitlab-ci-metrics-exporter.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}{{ .Values.config.metricsPath }}

{{- if .Values.route.enabled }}

Route:
  https://{{ if .Values.route.host }}{{ .Values.route.host }}{{ else }}<auto-generated-by-openshift>{{ end }}{{ .Values.config.metricsPath }}
{{- end }}

{{- if not .Values.auth.pushAuthToken }}
{{- if not .Values.auth.existingSecret }}

WARNING: no PUSH_AUTH_TOKEN configured (auth.pushAuthToken / auth.existingSecret).
The /metrics push endpoint is UNAUTHENTICATED. Set auth.pushAuthToken to enable
Bearer token authentication.
{{- end }}
{{- end }}

{{- if not .Values.persistence.enabled }}

NOTE: persistence is disabled. Pushed metrics are kept in memory only and
will be lost on pod restart. Set persistence.enabled=true to mount a PVC.
{{- end }}

Push a sample metric:

  curl -X POST "<route-or-service-url>{{ .Values.config.metricsPath }}" \
    -H "Content-Type: application/json" \
    {{- if or .Values.auth.pushAuthToken .Values.auth.existingSecret }}
    -H "Authorization: Bearer <PUSH_AUTH_TOKEN>" \
    {{- end }}
    -d '{"name":"example_metric","type":"gauge","value":1}'
